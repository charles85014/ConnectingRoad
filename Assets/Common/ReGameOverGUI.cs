using UnityEngine;
using System.Collections;

public class ReGameOverGUI : MonoBehaviour {
    public ReGui ReGUIScore;
    public float ScoreW_down;
	// Use this for initialization
	void Start () {
	
	}
    void OnGUI() {
        if (ReGUIScore.TimeCounter <= 0) { 
            
        
        }
    
    }
	// Update is called once per frame
	void Update () {
        
	}
}
